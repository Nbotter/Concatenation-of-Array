## Step1  
for,if文を駆使して配列に貯めておいて、それをもう一度踏んだらループありとする方法を思いつく
```
list = {}
for i in range
if 
list.append()
if list[i] == range
```

ほかの人のコードも見てみたが大体以下のようなコードを書いていた
```
seen = set()

while head:
    if head in seen:
        return True
    seen.add(head)
    head = head.next

return False
```
whileを使っているところは違うが、すでに訪れたものを配列に入れて次の値が含まれているかを見ているという点でほとんど自分の方針との差分がなかった

## Step2
ただ、これ自体有名な問題らしく調べてみるといろいろな記述があった  
コメント集を見に行くと、金属の輪っかと南京錠が連なっていることを連想して身体性を持てとのことだったが、これはStep1の解き方に相当するのだろう  
ただ、自分で絵を描いてみると「逆から走査しても絵を描けるか」というのも判定に使えそうだと思ったがアルゴリズムへの落とし込みかたがよくわからない  
ここまで書いていて思いついたのだが、陸上トラックを思い浮かべて互いに同じ速さで逆向きに走らせてぶつかればループありと判定してもよいのでは？  
とも考えた.  
この設定で同じ数だけ輪っかと南京錠のペアを通過したらループありと判定してもよさそうな気がする(円の対称性の利用)  
となると、以下のようにするのが大まかな指針か？  
```
num_node
```
よくわからなくなってきた
とにかく、色々な人がすでに解いていたのでそれを参考にしてみた
同じ方向に動いて追いついたらループありとしている解法が多かった  
ポインタをノードの組として想起した
```
class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        slow_pointer = head
        fast_pointer = head

        # ここで今のノードと次のノードが存在することを保証
        while fast_pointer is not None and fast_pointer.next is not None:
            # slowなポインタは1つだけ進む
            slow_pointer = slow_pointer.next
            # fastなポインタは2つ進む
            fast_pointer = fast_pointer.next.next
            # 追いついたらループあり
            if slow_pointer is fast_pointer:
                return True

        return False
```
## Step3  
Step1の解法について深めてみる  
まず、これは暗にサイズの固定はなされていないように思えるがリストでポインタを繋いでいくことを解法として想定しているからだろうか  
サイズが固定されているなら配列のほうがよさそう(?)
→この固定されている場合だとリングバッファというのがあるらしい
