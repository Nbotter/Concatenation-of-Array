## Step1
絵を描いて考えた  
自然言語化  
1)ソートされている。そして、重複した場合は削除  
これは連結リストの組み換えを行っているのでは？  
現在のnodeと次のnodeを比較して  
  a.)同一のオブジェクト  
  .nextでスキップする  
  b.)異なるオブジェクト  
  そのまま次のnodeをカウントする  
ということで以下のコード  
'''
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        current = head

        while current is not None and current.next is not None:
            if current.val == current.next.val:
                current.next = current.next.next
            else:
                current = current.next

        return head
'''
## Step2  
典型コメント集を見に行く
