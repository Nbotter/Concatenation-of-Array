# 思考ログ
66. Plus One

## Step 1-1  
digits[i]に対してi=1~3を考える
例えばdigits[0] = 2,digits[1] = 3,digits[2] = 1  
として、配列に入れるとしたら以下のようになるなと考える
[2,3,1]   
しかし、ここでこの配列を231という数値に直す方法が分からないなと考える
配列にある数字をどう数字に直すのかわからない   

## Step 1-2  
通常の筆算を思い出す  
これで以下の場合分けがあることに気づく  
1.digits[i] ≠ 9  
ならdigits[i] + 1

2.dig[i] = 9
ならさらに
a.最下位の桁のみ9(例:129)
b.すべての桁が9(例:999)

このあたりでAIにヒントを聞く

## Step3
次のように場合分けが行われる
1.9ない(一番右の桁に足し算が行われる)
2.9が含まれるなら繰り上がり(carry)が発生  
 a.9が連続して含まれる
 b.9しかない

しかし、2-bの場合は配列のサイズを増やさないといけないと思うがそれはどうすればよいのだろう…
## Step4
こんなpythonコードを書いてみた

```
#配列を右から左へ見る
for i in range (-1,0,-1):
#9じゃなければ、+1してreturn
  if dig[i_max] != 9
  elif dig[i_
```
